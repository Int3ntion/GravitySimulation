/**
 * @file PlanetInfoDialog.h
 * @brief Заголовок диалогового окна для отображения и редактирования информации о планете.
 *
 * Данный файл объявляет класс PlanetInfoDialog, наследующийся от QDialog.
 * Диалог предназначен для предоставления пользователю информации об объекте симуляции
 * и позволяет выполнить следующие действия:
 * - Просмотреть массу, координаты и скорость объекта (только для чтения).
 * - Изменить имя объекта.
 * - Инициировать удаление объекта из симуляции.
 *
 * Диалоговое окно является модальным, что гарантирует фокус внимания пользователя
 * на информации о конкретной планете до принятия решения.
 *
 * @note Класс зависит от структуры Object, объявленной в SimulationGLWidget.h.
 * Поля координат и скорости заблокированы для редактирования, так как их изменение
 * должно производиться исключительно физическим движком симуляции.
 *
 * @author Шестаков Денис Сергеевич
 */

#pragma once

#include <QDialog>
#include <QDoubleSpinBox>
#include <QHBoxLayout>
#include <QLabel>
#include <QLineEdit>
#include <QPushButton>
#include <QVBoxLayout>

#include "SimulationGLWidget.h"

/**
 * @class PlanetInfoDialog
 * @brief Модальное диалоговое окно для просмотра и частичного редактирования данных планеты.
 *
 * Предоставляет интерфейс для отображения детальной информации об объекте типа Object:
 * название, масса, векторы позиции и скорости. Позволяет переименовать планету или
 * запросить её удаление из симуляции.
 *
 * @inherit QDialog
 */
class PlanetInfoDialog : public QDialog
{
    Q_OBJECT

   public:
    /**
     * @brief Конструктор класса PlanetInfoDialog.
     *
     * Создает диалоговое окно, инициализирует его заголовок на основе имени планеты
     * и заполняет все поля интерфейса данными переданного объекта.
     * Настраивает компоновку элементов (вертикальная иерархия с горизонтальными группами
     * для координат и скорости).
     *
     * @param planet Константная ссылка на объект планеты (структура Object),
     *               данные которой будут отображены в диалоге.
     * @param parent Указатель на родительский виджет (по умолчанию nullptr).
     */
    explicit PlanetInfoDialog(const Object& planet, QWidget* parent = nullptr);

    /**
     * @brief Возвращает новое имя планеты, введенное пользователем в поле редактирования.
     *
     * Вызывается главным окном после закрытия диалога с результатом Accepted (OK),
     * чтобы получить актуальное имя для обновления объекта в симуляции.
     *
     * @return QString Строка с новым именем планеты. Если пользователь не менял имя,
     *                 возвращается исходное значение.
     */
    QString getNewName() const;

   private slots:
    /**
     * @brief Слот-обработчик нажатия кнопки "Удалить планету".
     *
     * Устанавливает специальный код возврата (-1) через метод done(), сигнализируя
     * вызывающему коду о необходимости удалить соответствующую планету из списка объектов.
     * После установки кода возврата диалог автоматически закрывается.
     */
    void onDeleteButtonClicked();

   private:
    /// @brief Поле ввода для редактирования имени планеты.
    QLineEdit* m_nameEdit;

    /// @brief Спинбокс для отображения массы планеты (только для чтения).
    QDoubleSpinBox* m_massSpin;

    /// @brief Спинбоксы для отображения координат планеты по осям X, Y, Z соответственно.
    QDoubleSpinBox *m_posXSpin, *m_posYSpin, *m_posZSpin;

    /// @brief Спинбоксы для отображения скорости планеты по осям X, Y, Z соответственно.
    QDoubleSpinBox *m_velXSpin, *m_velYSpin, *m_velZSpin;

    /// @brief Кнопка "Удалить планету", подключенная к слоту onDeleteButtonClicked.
    QPushButton* m_deleteButton;

    /// @brief Индекс планеты в исходном векторе симуляции.
    int m_planetIndex;
};